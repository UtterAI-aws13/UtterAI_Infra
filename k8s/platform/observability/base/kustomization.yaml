apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - otel-collector.yaml
  - opensearch.yaml
  - data-prepper.yaml
  - grafana-dashboard-ca-karpenter.yaml
  - grafana-dashboard-utterai.yaml
  - grafana-dashboard-pipeline.yaml
  - alert-rules-utterai.yaml
  - alertmanager-discord-secret.yaml
  - alertmanager-discord-config.yaml
  # alertmanager-slack-secret.yaml — Slack 미사용으로 비활성화
  # alertmanager-config.yaml       — Slack 미사용으로 비활성화
