apiVersion: apps/v1
kind: Deployment
metadata:
  name: utterai-cpu-worker
  namespace: utterai-ai-cpu
spec:
  # KEDA가 관리하므로 초기 replica=0
  replicas: 0
  selector:
    matchLabels:
      app: utterai-cpu-worker
  template:
    metadata:
      labels:
        app: utterai-cpu-worker
    spec:
      serviceAccountName: utterai-cpu-worker-sa
      nodeSelector:
        workload: worker
      terminationGracePeriodSeconds: 60
      containers:
        - name: worker
          image: ${AWS_ACCOUNT_ID}.dkr.ecr.ap-northeast-2.amazonaws.com/utterai-ai-cpu:latest
          command: ["python", "-m", "app.workers.cpu_worker"]
          env:
            - name: AWS_REGION
              value: "ap-northeast-2"
            - name: SQS_CPU_QUEUE_URL
              value: "https://sqs.ap-northeast-2.amazonaws.com/${AWS_ACCOUNT_ID}/utterai-dev-cpu-analysis-queue"
            - name: SQS_ML_GPU_QUEUE_URL
              value: "https://sqs.ap-northeast-2.amazonaws.com/${AWS_ACCOUNT_ID}/utterai-dev-audio-ml-queue"
            - name: S3_RAW_AUDIO_BUCKET
              value: "utterai-dev-raw-audio"
            - name: S3_PROCESSED_AUDIO_BUCKET
              value: "utterai-dev-processed-audio"
            - name: LOG_LEVEL
              value: "DEBUG"
          resources:
            requests:
              cpu: "1"
              memory: "2Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          # Spot 중단 신호를 받으면 현재 메시지 처리 후 종료
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
